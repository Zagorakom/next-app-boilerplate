/**** Examples -> ****/
/*
export type TExample = 'xl' | 'sm';

export type TExampleObject = {
	name: string;
	age: number;
	address: {
		street: string;
		city: string;
		country: string;
	};
};

export enum EExample {
    Edit = 'edit',
	Add = 'add'
}

export interface IExample {
	created_at: string;
	deleted_at: null | string;
	id: number;
	name: string;
	shortname: string;
	status: number;
	updated_at: string;
}
*/
/**** <- Examples ****/

export enum ESpinnerVariants {
    Primary = 'primary',
    Secondary = 'secondary',
    Success = 'success',
    Danger = 'danger',
    Warning = 'warning',
    Info = 'info',
    Light = 'light',
    Dark = 'dark',
}
